@model IEnumerable<AnotherProject.Models.SubProduct>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NewSubProductName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NewSubProductName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubProductID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubProductID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubProductID })
        </td>
    </tr>
}

</table>
